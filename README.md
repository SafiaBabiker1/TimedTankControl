# TimedTankControl
I built this Factory I/O logic using Control I/O. It features a fill/discharge system with an adjustable setpoint (currently around 95 units). I added a "memory" circuit that freezes the timer, so the process time stays visible on the screen even after the valves stop.
